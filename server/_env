# This file documents possible server configuration without
# the risk of checking sensitive variables into the repository.
# Rename it to `.env` and then feel free to add sensitive values as `.env` is
# in the `.gitignore`.

# The environment
NODE_ENV=development

# The ghost environment
GHOST_ENV=development

# Show debugger output in terminal
# https://github.com/visionmedia/debug#wildcards
DEBUG=app, webpack-universal-renderer-middleware, ghost-middleware

# Server listening port
PORT=6060

# Any valid ms value
# https://github.com/rauchg/ms.js
BROWSER_CACHE=0

# Whether to serve client side assets using webpack-dev-middleware middleware
# instead of /dist.
WEBPACK_DEV_SERVER=true

BROWSER=false
CLIENT_ENV=development
API_ENDPOINT=http://localhost:6060/api
GHOST_API_ENDPOINT=http://localhost:6060/ghost/api/v0.1
GHOST_API_CLIENT_ID=ghost-frontend
GHOST_API_CLIENT_SECRET=06b9f5556fda
DISQUS_SHORTNAME=richardscarrott-dev
BLOG_ENABLED=false
